# Add script to:
# * Install dependencies
# * Build/Compile
# * Run Test Suit to validate
#
# After this is run, bin/parking_lot
# should Just Work.

# test suite 
cd $(pwd)/src/test 
go test -count=1 -v  
cd ../..
# run the application
bin/parking_lot 

 